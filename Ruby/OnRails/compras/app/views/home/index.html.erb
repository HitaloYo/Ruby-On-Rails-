<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YoYo Compras - Página Inicial</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #4b6cb7;
            --secondary: #182848;
            --accent: #FE6B8B;
            --accent-gradient: linear-gradient(45deg, #FF8E53, #FE6B8B);
            --light: #f5f7fa;
            --dark: #333;
            --gray: #555;
            --border-radius: 12px;
            --box-shadow: 0 5px 20px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            color: var(--dark);
            min-height: 100vh;
            line-height: 1.6;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }
        
        /* NOVA NAVBAR - Design Moderno */
        header {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            color: var(--dark);
            padding: 15px 0;
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
            margin-bottom: 30px;
            position: sticky;
            top: 0;
            z-index: 100;
            border-bottom: 1px solid rgba(75, 108, 183, 0.1);
        }
        
        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--primary);
            text-decoration: none;
            transition: var(--transition);
            margin-right: 30px; /* Espaçamento aumentado entre logo e navegação */
        }
        
        .logo:hover {
            transform: translateY(-2px);
        }
        
        .logo i {
            color: #FFD700;
            font-size: 2rem;
            background: linear-gradient(45deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        
        /* Navegação moderna */
        nav ul {
            display: flex;
            list-style: none;
            gap: 8px; /* Aumentei o gap entre os itens */
            flex-wrap: wrap;
            align-items: center;
        }
        
        nav li {
            border-radius: var(--border-radius);
            transition: var(--transition);
            position: relative;
            margin: 0 2px; /* Pequeno espaçamento entre os itens */
        }
        
        nav li:not(.btn-container):hover {
            background: rgba(75, 108, 183, 0.05);
        }
        
        nav a {
            color: var(--dark);
            text-decoration: none;
            padding: 12px 18px;
            display: flex;
            align-items: center;
            gap: 8px;
            font-weight: 500;
            border-radius: var(--border-radius);
            transition: var(--transition);
        }
        
        nav a i {
            font-size: 1.1rem;
            width: 20px;
            text-align: center;
        }
        
        nav a:hover {
            color: var(--primary);
            transform: translateY(-2px);
        }
        
        .ativo {
            background: rgba(255, 215, 0, 0.15);
            color: var(--primary);
            font-weight: 600;
        }
        
        .ativo::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 20%;
            width: 60%;
            height: 3px;
            background: var(--accent-gradient);
            border-radius: 3px;
        }
        
        .btn-creme {
            background: var(--accent-gradient);
            color: white;
            padding: 12px 22px;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            font-weight: 600;
            font-size: 0.95rem;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: var(--transition);
            box-shadow: 0 4px 15px rgba(254, 107, 139, 0.3);
            margin-left: 5px; /* Espaçamento à esquerda do botão */
        }
        
        .btn-creme:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(254, 107, 139, 0.4);
        }
        
        /* Conteúdo principal (mantido do original) */
        .main-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-bottom: 40px;
        }
        
        .card {
            background: white;
            padding: 30px;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
            height: 100%;
        }
        
        .welcome-section h2 {
            color: var(--primary);
            margin-bottom: 15px;
            font-size: 1.8rem;
        }
        
        .welcome-section p {
            color: var(--gray);
            margin-bottom: 20px;
        }
        
        .features {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            margin-top: 20px;
        }
        
        .feature {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            transition: var(--transition);
        }
        
        .feature:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }
        
        .feature i {
            font-size: 2rem;
            color: var(--primary);
            margin-bottom: 10px;
        }
        
        .feature h3 {
            font-size: 1.1rem;
            margin-bottom: 8px;
        }
        
        .feature p {
            font-size: 0.9rem;
            color: var(--gray);
        }
        
        /* Seção de receitas melhorada */
        .recipes-section h2 {
            color: var(--primary);
            margin-bottom: 20px;
            font-size: 1.8rem;
            text-align: center;
        }
        
        .recipe-cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }
        
        .recipe-card {
            background: linear-gradient(135deg, #fdfcfb 0%, #e2d1c3 100%);
            border-radius: 10px;
            overflow: hidden;
            transition: var(--transition);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
        }
        
        .recipe-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
        }
        
        .recipe-img {
            height: 160px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 3rem;
        }
        
        .recipe-content {
            padding: 20px;
            flex-grow: 1;
            display: flex;
            flex-direction: column;
        }
        
        .recipe-content h3 {
            color: var(--secondary);
            margin-bottom: 10px;
        }
        
        .recipe-content p {
            color: #666;
            font-size: 0.9rem;
            margin-bottom: 15px;
            flex-grow: 1;
        }
        
        .btn-recipe {
            background: var(--primary);
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            width: 100%;
            transition: var(--transition);
            font-weight: 500;
        }
        
        .btn-recipe:hover {
            background: var(--secondary);
        }
        
        /* Cores específicas para cada receita */
        .creme .recipe-img {
            background: linear-gradient(45deg, #4b6cb7, #182848);
        }
        
        .pudim .recipe-img {
            background: linear-gradient(45deg, #FF8E53, #FE6B8B);
        }
        
        .lasanha .recipe-img {
            background: linear-gradient(45deg, #C36A2D, #E2C391);
        }
        
        .churrasco .recipe-img {
            background: linear-gradient(45deg, #8B0000, #FF2400);
        }
        
        /* Banner promocional melhorado */
        .promo-banner {
            background: var(--accent-gradient);
            color: white;
            padding: 25px;
            border-radius: var(--border-radius);
            text-align: center;
            margin-bottom: 40px; /* Aumentei o margin-bottom para mais espaçamento */
            box-shadow: var(--box-shadow);
            margin-top: 10px; /* Adicionei margem no topo também */
        }
        
        .promo-banner h2 {
            font-size: 2.2rem; /* Aumentei ligeiramente o tamanho da fonte */
            margin-bottom: 15px; /* Aumentei o espaçamento inferior */
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 15px; /* Aumentei o espaçamento entre as estrelas e o texto */
            flex-wrap: wrap;
        }
        
        .promo-banner p {
            font-size: 1.2rem; /* Aumentei ligeiramente o tamanho da fonte */
            margin-top: 10px;
        }
        
        /* Footer melhorado */
        footer {
            text-align: center;
            padding: 30px 0;
            margin-top: 40px;
            color: var(--gray);
            border-top: 1px solid #ddd;
        }
        
        footer p {
            margin-bottom: 10px;
        }
        
        /* Menu mobile */
        .menu-toggle {
            display: none;
            flex-direction: column;
            justify-content: space-around;
            width: 30px;
            height: 25px;
            background: transparent;
            border: none;
            cursor: pointer;
            padding: 0;
        }
        
        .menu-toggle span {
            width: 100%;
            height: 3px;
            background-color: var(--primary);
            border-radius: 2px;
            transition: var(--transition);
        }
        
        /* Responsividade */
        @media (max-width: 1024px) {
            nav ul {
                gap: 6px;
            }
            
            nav a {
                padding: 10px 15px;
                font-size: 0.9rem;
            }
            
            .btn-creme {
                padding: 10px 18px;
                font-size: 0.9rem;
            }
            
            .logo {
                margin-right: 20px; /* Reduzi o espaçamento em telas menores */
            }
        }
        
        @media (max-width: 900px) {
            .main-content {
                grid-template-columns: 1fr;
            }
            
            .header-content {
                flex-direction: column;
                gap: 15px;
            }
            
            nav ul {
                justify-content: center;
            }
            
            .features {
                grid-template-columns: 1fr;
            }
            
            .menu-toggle {
                display: flex;
                position: absolute;
                top: 20px;
                right: 20px;
            }
            
            nav ul {
                display: none;
                flex-direction: column;
                width: 100%;
                gap: 10px;
                margin-top: 15px;
            }
            
            nav ul.active {
                display: flex;
            }
            
            nav li {
                width: 100%;
                text-align: center;
            }
            
            .ativo::after {
                display: none;
            }
            
            .logo {
                margin-right: 0; /* Remove margem em layout mobile */
                margin-bottom: 10px;
            }
        }
        
        @media (max-width: 768px) {
            .logo {
                font-size: 1.6rem;
            }
            
            .logo i {
                font-size: 1.8rem;
            }
            
            .btn-creme {
                justify-content: center;
            }
            
            .recipe-cards {
                grid-template-columns: 1fr;
            }
            
            .promo-banner h2 {
                flex-direction: column;
                gap: 8px; /* Ajuste do gap para layout em coluna */
            }
            
            .promo-banner {
                padding: 20px;
                margin-bottom: 30px;
            }
        }

        /* Animações suaves */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .animate {
            animation: fadeIn 0.6s ease-out forwards;
        }
        
        .delay-1 { animation-delay: 0.1s; }
        .delay-2 { animation-delay: 0.2s; }
        .delay-3 { animation-delay: 0.3s; }
        .delay-4 { animation-delay: 0.4s; }
        
        /* Efeito de foco para melhor acessibilidade */
        button:focus, a:focus {
            outline: 2px solid var(--primary);
            outline-offset: 2px;
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <div class="header-content">
                <a href="/" class="logo">
                    <i class="fas fa-shopping-cart"></i>
                    <span>YoYo Compras</span>
                </a>
                
                <button class="menu-toggle" aria-label="Abrir menu">
                    <span></span>
                    <span></span>
                    <span></span>
                </button>
                
                <nav>
                    <ul>
                        <li class="ativo"><a href="/"><i class="fas fa-home"></i> Home</a></li>
                        <li><a href="/compas"><i class="fas fa-calendar-alt"></i> Compras</a></li> 
                    </ul>
                </nav>
            </div>
        </div>
    </header>

    <div class="container">
        <div class="promo-banner animate">
            <h2><i class="fas fa-star"></i> Bem-vindo à YoYo Compras! <i class="fas fa-star"></i></h2>
            <p>Seu assistente pessoal para compras e receitas deliciosas</p>
        </div>
        
        <div class="main-content">
            <div class="welcome-section card animate">
                <h2>Bem-vindo ao YoYo Compras!</h2>
                <p>Seu assistente pessoal para compras e receitas deliciosas. Aqui você pode planejar suas compras mensais e encontrar receitas incríveis para fazer em casa.</p>
                
                <div class="features">
                    <div class="feature delay-1">
                        <i class="fas fa-list"></i>
                        <h3>Listas de Compras</h3>
                        <p>Organize suas compras do mês</p>
                    </div>
                    <div class="feature delay-2">
                        <i class="fas fa-receipt"></i>
                        <h3>Receitas</h3>
                        <p>Pratos incríveis para toda família</p>
                    </div>
                    <div class="feature delay-3">
                        <i class="fas fa-calculator"></i>
                        <h3>Orçamento</h3>
                        <p>Controle seus gastos</p>
                    </div>
                    <div class="feature delay-4">
                        <i class="fas fa-bell"></i>
                        <h3>Lembretes</h3>
                        <p>Não esqueça itens importantes</p>
                    </div>
                </div>
            </div>
            
            <div class="recipes-section card animate">
                <h2>Receitas em Destaque</h2>
                <div class="recipe-cards">
                    <div class="recipe-card creme delay-1">
                        <div class="recipe-img">
                            <i class="fas fa-drumstick-bite"></i>
                        </div>
                        <div class="recipe-content">
                            <h3>Creme de Galinha</h3>
                            <p>Um creme delicioso e fácil de preparar para impressionar sua família.</p>
                            <button onclick="window.location.href='/creme'" class="btn-recipe">Ver Receita</button>
                        </div>
                    </div>
                    
                    <div class="recipe-card pudim delay-2">
                        <div class="recipe-img">
                            <i class="fas fa-ice-cream"></i>
                        </div>
                        <div class="recipe-content">
                            <h3>Pudim</h3>
                            <p>O clássico pudim de leite condensado que todo mundo adora.</p>
                            <button onclick="window.location.href='/receitas/pudim'" class="btn-recipe">Ver Receita</button>
                        </div>
                    </div>
                    
                    <div class="recipe-card lasanha delay-3">
                        <div class="recipe-img">
                            <i class="fas fa-bacon"></i>
                        </div>
                        <div class="recipe-content">
                            <h3>Lasanha</h3>
                            <p>Lasanha de carne que derrete na boca. Perfeita para domingos.</p>
                            <button onclick="window.location.href='/receitas/lasanha'" class="btn-recipe">Ver Receita</button>
                        </div>
                    </div>
                    
                    <div class="recipe-card churrasco delay-4">
                        <div class="recipe-img">
                            <i class="fas fa-fire"></i>
                        </div>
                        <div class="recipe-content">
                            <h3>Churrasco</h3>
                            <p>Cortes selecionados e marinadas especiais para um churrasco perfeito.</p>
                            <button onclick="window.location.href='/receitas/churrasco'" class="btn-recipe">Ver Receita</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <footer>
        <div class="container">
            <p>© 2023 YoYo Compras - Todos os direitos reservados</p>
            <p>Desenvolvido com <i class="fas fa-heart" style="color: #FE6B8B;"></i> para amantes de boa comida</p>
        </div>
    </footer>

    <script>
        // Adiciona classes de animação aos elementos quando a página carrega
        document.addEventListener('DOMContentLoaded', function() {
            const elementsToAnimate = document.querySelectorAll('.animate');
            
            elementsToAnimate.forEach(element => {
                // Garante que elementos com delay não sejam mostrados antes da animação
                element.style.opacity = '0';
            });
            
            // Pequeno delay para garantir que o CSS seja aplicado primeiro
            setTimeout(() => {
                elementsToAnimate.forEach(element => {
                    element.style.opacity = '1';
                });
            }, 50);
            
            // Menu mobile toggle
            const menuToggle = document.querySelector('.menu-toggle');
            const navMenu = document.querySelector('nav ul');
            
            if (menuToggle && navMenu) {
                menuToggle.addEventListener('click', function() {
                    navMenu.classList.toggle('active');
                    
                    // Animações dos spans do menu hamburger
                    const spans = menuToggle.querySelectorAll('span');
                    if (navMenu.classList.contains('active')) {
                        spans[0].style.transform = 'rotate(45deg) translate(5px, 5px)';
                        spans[1].style.opacity = '0';
                        spans[2].style.transform = 'rotate(-45deg) translate(7px, -6px)';
                    } else {
                        spans[0].style.transform = 'none';
                        spans[1].style.opacity = '1';
                        spans[2].style.transform = 'none';
                    }
                });
            }
        });
    </script>
</body>
</html>